#define  _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>

int main()
{
    int x = 0;
    int y = 0;
    scanf("%d", &x);
    if (x < 0)
        printf("1", y);
    else if(x == 0)
        printf("0", y);
    else
    printf("-1", y);
    return 0;
}